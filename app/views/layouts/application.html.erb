<!DOCTYPE HTML>
<html lang="pt-BR" dir="ltr">
  <head>
    <title><%= default_title %></title>
    <meta charset="utf-8" />
    <meta name="description" content="<%= default_description %>" />
    <meta property="og:image" content="<%= default_og_image %>" />
    <%= csrf_meta_tag %>
    <%= favicon_link_tag %>
    <%= stylesheet_link_tag 'fonts/open_sans' %>
    <%= stylesheet_link_tag 'application' %>
    <!--[if lt IE 9]><%= javascript_include_tag 'html5' %><![endif]-->
  </head>
  <body>
    <div class="main-container">
      <header class="main">
        <div class="container">
          <h1 class="logo">
            <%= link_to image_tag('layout/logo.png', alt: default_title), root_path, title: default_title %>
          </h1>
          <nav>
            <ul>
              <li><%= link_to ENV['SMTP_ADDRESS'], 'mailto:fale-com@sirva.me', title: 'Envie-nos um e-mail. Ficaremos felizes!' %></li>
            </ul>
          </nav>
        </div>
      </header>
      <section>
        <%= yield %>
      </section>
      <footer>
        <div class="container">
          <%= link_to image_tag('layout/logo1.png', alt: default_title), root_path, title: default_title, class: 'logo' %>
          <%= link_to 'who', 'http://bazzarella.com', target: '_blank', title: 'Felipe Bazzarella', class: 'who' %>
        </div>
      </footer>
    </div>
    <%= render './metrics' %>
    <%= javascript_include_tag 'application' %>
  </body>
</html>