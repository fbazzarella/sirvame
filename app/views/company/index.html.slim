/!== render partial: './advertise'
header.body.search
    div.container
        h3 Encontre o que precisa:
        div.input-prepend
            span.add-on = image_tag 'zoom_tool.png', alt: 'Lupa'
            input type='text' placeholder='Exemplos: pizza, lanche, bar do zé... e aperte ENTER!'
        == render partial: './addthis'
/!header.body.company
    div.container
        = link_to '« Ver Catálogo', '/#', title: 'Voltar ao Catálogo Comercial', class: 'back-to-catalog'
        h2 Padaria Ki-Sabor
        == render partial: './addthis'
div.container.main-content
    ul.search-results
        - if @companies.any?
            - @companies.each do |company|
                li
                    p = company.name
                    p = company.phone
                    p = company.address
                    p = "#{company.segment_list} [#{company.product_list}]"
        - else
            p.none-companies Nenhuma empresa encontrada.