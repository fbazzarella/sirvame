ul.search-results
	- if @companies.any?
	    - @companies.each do |company|
	        li data-id="#{company.id}" class="#{'plus' if company.plus}"
	        	= link_to image_tag('layout/see_details.png'), '#', class: 'see-details'
	            p.name = company.name
	            p.phone = company.phone
	            p.address = company.address
	            p.tags = "#{company.segment_list} [#{company.product_list}]"
	- else
	    li data-id="0"
	    		p.none-companies Nenhuma empresa encontrada.