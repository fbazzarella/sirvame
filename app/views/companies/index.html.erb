<header class="search">
	<form action="/" method="get" class="container">
		<h2>Encontre o que precisa em Valença</h2>
		<div>
			<input id="search-field" type="text" name="encontrar" value="<%= normalize_search_params(params[:encontrar]) %>" placeholder="Exemplos: água, pizza, lanchonete, academia... e clique na lupa »" />
			<span class="add-on">
				<%= image_submit_tag 'layout/zoom_tool.png', alt: 'Encontrar', id: 'submit-search' %>
			</span>
		</div>
		<%= render 'addthis' %>
	</form>
</header>
<div class="container search main-content">
	<p id="companies-count">
		Já temos um total de <strong><%= Company.count %></strong> empresas cadastradas.
		Quer se destacar? <%= default_contact_email 'Entre em contato', :inverse %> e conheça as vantagens.
	</p>
	<%= render 'search_results' %>
</div>